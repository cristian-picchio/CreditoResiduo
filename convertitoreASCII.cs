using System;

namespace esconvertitoreascii
{
    class Program
    {
        static void Main(string[] args)
        {
            string input = "D";
            {
                // get the integral value of character.
                int value = Convert.ToInt32(letter);
                // convert the decimal value to a hexadecimal value in string form.

                Console.WriteLine("hexadecimal value of {0} is {1}", letter, value);
            }

        }
    }
}
